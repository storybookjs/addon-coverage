export const ADDON_ID = "storybook/addon-coverage";
export const PANEL_ID = `${ADDON_ID}/panel`;
export const PARAM_KEY = `coverage`;

export const EVENTS = {
  COVERAGE_DETAIL: `${ADDON_ID}/COVERAGE_DETAIL`,
};
